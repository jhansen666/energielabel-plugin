{% for property in item.data.properties if property.group.id == 60 %}  {# X = ID des Merkmals #}
    <span>{{ property.names.name }}</span>  {# für den Wert eines Merkmals vom Typ Auwahl #}
{% endfor %} 
